{
  "className": "org.joget.apps.form.lib.Radio",
  "properties": {
    "id": "{{ id }}",
    "label": "{{ label }}",
    "required": "{{ required }}",
    "readonly": "{{ readonly }}",
    "optionsBinder": {{ options_binder | tojson_pretty }}
  }
}
