{
  "className": "org.joget.apps.form.lib.CheckBox",
  "properties": {
    "id": "{{ id }}",
    "label": "{{ label }}",
    "required": "{{ required }}",
    "readonly": "{{ readonly }}",
    "optionsBinder": {{ options_binder | tojson_pretty }}
  }
}
